# 面向 AI 编程方法论：用户提需求，AI 交付成品，用户负责验收

> 这套方法适合由普通用户、产品提出者或具备少量编程基础的人主导项目。用户不需要亲自完成全部技术设计；用户负责说明真实需求、观察实际效果和反馈问题，AI 负责理解项目、研究案例、实现源码、自动测试、构建程序、编写验收教程和维护文档。

## 一、核心思想

面向 AI 编程不是“让 AI 随便生成一段代码”，而是建立一个稳定的成品循环：

```text
用户描述真实需求
        ↓
AI 核对项目与成功案例
        ↓
AI 直接完成可运行成品
        ↓
AI 先做自动检查和构建
        ↓
用户按教程进行真实验收
        ↓
用户只反馈不正确的部分
        ↓
AI 精准修正，直到通过
        ↓
更新文档并归档版本
```

最重要的原则是：

> 用户说清楚“想得到什么效果”，AI 负责把它变成可以运行、可以测试、可以维护的成品；用户不需要替 AI 拆解全部技术细节。

## 二、双方的职责

### 用户负责什么

1. 描述自己的使用场景和最终想看到的效果。
2. 说明哪些功能最重要，哪些明确不要。
3. 提供截图、视频、报错时间、成功案例或参考软件。
4. 使用 AI 做好的正式程序进行真实环境验收。
5. 只指出失败项；没有提到的项目可以明确写“其余默认通过”。
6. 决定何时提交、上传、发布或进入下一阶段。

### AI 负责什么

1. 先读取项目的最高指令、当前需求、路线图和交接文档。
2. 保护用户已有文件，尤其是脚本、配置、日志、录像和未提交修改。
3. 查找现有源码、优秀案例、官方资料或可信开源实现。
4. 把用户语言转换成清楚、统一、可测试的技术契约。
5. 直接完成源码、自动测试、文档和正式构建，不只给方案或代码片段。
6. 先用假输入、模拟依赖或离线测试验证，禁止拿用户真实环境冒险。
7. 提供面向小白的中文验收教程，等待用户真实验收。
8. 失败后依据证据精准修复，不随意重构已通过部分。
9. 如实区分“自动测试通过”“程序已构建”和“用户真实验收通过”。
10. 未经用户明确授权，不提交、不推送、不发布，更不能强推。

## 三、每一轮固定工作流程

### 第 1 步：锁定一个小目标

AI 先把本轮工作压缩成一句话，例如：

```text
本轮目标：增加鼠标 X/Y 相对移动，并完成自动测试、正式构建和 Windows 验收教程。
```

同时写清范围外：

```text
本轮不做：绝对坐标、轨迹录制、滚轮、图像识别和驱动输入。
```

这样可以防止开发过程中不断扩大范围，也方便用户判断 AI 是否理解正确。

### 第 2 步：给出 READY 判断

需求清楚时，AI 用很短的话确认理解并立即开工：

```text
READY：目标、边界和验收标准已经清楚。我会直接实现源码、测试、文档和正式程序，完成后交给你验收。
```

只有会明显改变成品方向的关键事实不清楚时，AI 才暂停询问。能从项目、截图、日志或案例中查到的事情，不应反复让用户回答。

### 第 3 步：先找证据，再写代码

优先级如下：

1. 当前项目已经通过验收的源码和测试。
2. 用户指定的“优秀案例”或参考项目。
3. 官方文档和官方源码。
4. 可信的开源项目。
5. 最后才是基于通用知识自行设计。

研究案例时只吸收以下内容：

- 行为和交互逻辑；
- 参数语义和边界；
- 底层技术路线；
- 停止、异常和清理机制；
- 可验证的性能与安全设计。

不要直接复制案例品牌、素材、整个源码、私有格式或用户没有要求的功能。

### 第 4 步：把自然语言变成验收契约

AI 应把需求整理成“输入—行为—结果—边界”。例如：

| 项目 | 明确契约 |
|---|---|
| 输入 | `x`、`y` 为整数 |
| 行为 | 相对当前位置移动 |
| 方向 | X 正数向右，Y 正数向下 |
| 停止 | 松开触发键后停止剩余步骤 |
| 不做 | 不读取当前位置，不使用绝对坐标 |

好的契约必须可以直接转成自动测试和人工验收步骤。

### 第 5 步：直接交付完整成品

只要需求已经明确，AI 应一次性完成力所能及的全部内容：

- 功能源码；
- 参数校验和错误提示；
- 自动测试；
- 编译检查；
- 用户可见说明；
- README 或版本更新；
- 正式程序构建；
- 中文验收文档；
- 当前进度和交接文档。

不要把本可一次完成的工作人为拆成很多轮，也不要只返回伪代码让用户自己拼装。

### 第 6 步：自动检查不能代替真实验收

AI 先完成自动检查：

| 检查 | 目的 |
|---|---|
| 单元测试 | 验证参数、状态、停止和边界 |
| 回归测试 | 防止旧功能被破坏 |
| 编译检查 | 排除语法和导入错误 |
| 差异检查 | 排除空白错误、误改和无关文件 |
| 正式构建 | 生成用户真正会运行的程序 |
| 残留进程检查 | 防止构建或程序退出后仍占用资源 |

但必须明确：

```text
自动测试通过 ≠ 用户真实环境已经验收通过
构建成功 ≠ 游戏、硬件或 GUI 实际效果已经确认
```

真实鼠标、键盘、声音、显示器、游戏兼容性和视觉效果应由用户亲自验收。

### 第 7 步：给用户一份漂亮的小白验收教程

验收文档应包括：

1. 安全准备。
2. 正式程序的准确路径。
3. 按顺序编号的步骤。
4. 每一步的正确结果。
5. 最终通过标准。
6. 日志或输出文件位置。
7. 未覆盖范围。
8. 可直接复制的失败反馈模板。

推荐格式：

| 编号 | 用户操作 | 正确结果 | 用户结论 |
|---:|---|---|---|
| 1 | 点击开始 | 状态灯开始闪烁 | 通过 / 失败 |
| 2 | 执行测试动作 | 方向和时长正确 | 通过 / 失败 |
| 3 | 点击停止 | 资源停止且没有保存 | 通过 / 失败 |

这样用户只需回复“第 2 项失败，第 1、3 项通过”，沟通成本很低。

### 第 8 步：只修失败项，保护已通过部分

用户反馈时建议使用：

```text
第 1 项：通过。
第 2 项：失败，实际结果是……
第 3～6 项：默认通过。
新增建议：……
```

AI 收到反馈后应：

1. 把“已通过”视为回归保护范围。
2. 只修改失败项和用户新授权的需求。
3. 不因为一个小问题重做整个界面或架构。
4. 根据截图、日志和时间点找证据，不猜测原因。
5. 修复后重新运行相关测试和必要回归。
6. 更新验收教程，继续等待用户确认。

### 第 9 步：验收后再归档

状态必须清楚区分：

```text
实现中
→ 实现完成，等待人工验收
→ 用户验收通过
→ 已提交
→ 已推送或已发布
```

只有用户明确说“验收通过”后，才能写成已验收。只有用户明确要求上传或发布后，AI 才能操作 Git。

发布前建议核对：

- 工作区有哪些用户改动；
- 自动测试和构建是否通过；
- 本地提交 SHA；
- 远端目标分支 SHA；
- 推送期间用户是否又修改了文件；
- 是否误带日志、录像、设备路径或隐私配置。

默认使用普通推送，禁止强推覆盖历史。

## 四、用户怎样高效描述需求

用户不必写专业规格，只需尽量说明以下信息：

```markdown
# 本轮要实现什么
我希望……

# 使用场景
我会在……情况下使用它。

# 我想看到的结果
1. ……
2. ……
3. ……

# 当前问题
现在实际表现是……

# 参考案例
可以参考……，源码位于……

# 明确不要
不要调用……，不要实现……

# 验收方式
请完成程序并给我一份编号清楚的中文验收教程，我来实际测试。
```

截图、视频和日志不需要由用户分析。用户只要指出“哪里看起来不对”和大致发生时间，AI 应负责定位代码和日志证据。

## 五、AI 应怎样汇报进度

工作时间较长时，AI 应提供简短、可验证的中间更新，而不是长时间沉默：

```text
已确认：……
正在处理：……
下一步：……
当前没有修改：用户宏、配置和录像。
```

好的进度更新包含证据和下一步；不需要重复整份计划，也不要把尚未完成的事情说成完成。

最终汇报应先说结果：

1. 成品在哪里。
2. 实现了什么。
3. 自动检查结果。
4. 有哪些已知边界或未覆盖项。
5. 验收文档在哪里。
6. 当前状态是等待验收、已验收还是已发布。

## 六、项目中应长期维护的最小文档

建议每个项目只保留少量职责清楚的当前文档：

| 文档 | 唯一职责 |
|---|---|
| `AGENTS.md` | AI 必须遵守的最高规则、安全边界和工作方式 |
| `PRODUCT_REQUIREMENTS.md` | 当前仍有效的全部产品需求 |
| `PROJECT_ROADMAP.md` | 阶段、状态和唯一下一步 |
| `README.md` | 普通用户如何认识、运行和使用项目 |
| `CURRENT_HANDOVER.md` | 下一位 AI 接手所需的当前事实和证据 |
| 当前验收文档 | 本轮用户如何进行人工验收 |

不要同时维护多份互相冲突的路线图、需求文档和状态表。旧历史交给 Git 标签和提交保存，当前文档只描述现在。

## 七、安全与信任边界

AI 在任何项目中都应遵守：

1. 先运行 `git status --short`，识别并保护用户未提交改动。
2. 不恢复、覆盖、执行或删除用户文件，除非用户明确授权。
3. 测试外部输入时优先注入假函数，禁止自动测试真实操作用户电脑。
4. 安装工具必须使用明确、固定、可清理的路径，并记录用途和卸载影响。
5. 不把日志、录像、令牌、设备名称和本机路径误传到公开仓库。
6. 删除、覆盖主干、发布、外部发送等高影响操作必须得到明确授权。
7. 发现未知事实时写 `UNKNOWN`，不要用猜测伪装成已确认结论。
8. 与在线游戏、安全软件或反作弊相关时，不注入、不绕过、不隐瞒风险。

## 八、最容易降低效率的做法

### 只写方案，不做成品

用户已经授权实现且需求清楚时，AI 仍只给建议或伪代码，会把工作重新推给用户。正确做法是完成源码、测试、构建和教程。

### 把任务拆成过多轮次

如果源码、测试、文档和构建能够一轮完成，就应合并交付。只有需要用户真实视觉选择或外部条件时才设置阶段门。

### 未看项目和案例就猜

这会造成接口、UI、命名和安全逻辑偏移。应先检查当前项目和用户指定案例，再设计。

### 用自动测试冒充用户验收

离屏测试不能证明真实游戏、声音、录屏、任务栏、托盘或鼠标效果正确。必须等待用户确认。

### 修一个问题却破坏已通过部分

每次用户说“其余通过”时，那些功能都应成为回归保护范围。修改越小、证据越明确，项目越稳定。

### 未经授权就提交或上传

完成代码不等于获得发布授权。实现、验收和发布是三个不同状态。

## 九、可直接交给下一位 AI 的最高工作提示词

将下面整段复制到其他项目中，再补充项目路径和本轮需求即可：

```markdown
# 面向 AI 编程工作方式

你是本项目的完整交付负责人。用户负责描述真实需求和进行最终人工验收；你负责把需求变成可运行、可测试、可维护的成品。

## 开工规则

1. 先读取项目最高指令、当前需求、路线图、README 和当前交接，并运行 `git status --short`。
2. 保护用户全部未提交文件、脚本、配置、日志和产物，不得擅自恢复、覆盖、执行或删除。
3. 先用一句话锁定本轮目标和范围外，再给出简短 READY 判断。
4. 需求明确时直接实施，不要只写方案，不要把可一次完成的工作人为拆成很多轮。
5. 遇到技术难题时，先查当前源码和用户指定的优秀案例，再查官方资料或可信开源实现；只适配行为，不复制品牌、素材、私有格式或无关功能。

## 完整交付要求

一次性完成力所能及的：源码、参数校验、自动测试、回归、编译检查、用户说明、README/版本更新、正式构建、中文验收文档和当前交接。

自动测试必须优先使用假输入或模拟依赖，不得真实控制用户鼠标、键盘或破坏数据。自动测试与构建成功后，只能写“实现完成，等待人工验收”；用户明确确认前不得写“已验收”。

## 与用户沟通

工作较长时用简短更新说明：已确认什么、正在做什么、下一步是什么、哪些用户文件没有修改。最终先给出成品路径和结果，再给测试证据、已知边界和漂亮的编号验收教程。

用户反馈“第几项失败、其余通过”后，只修失败项和新授权需求，把已通过部分作为回归保护范围。根据截图、日志和时间证据定位，不猜测，不擅自扩大功能。

## Git 与发布

默认不提交、不推送、不发布。只有用户明确授权后才能操作 Git；普通推送，禁止强推。发布前核对工作区、本地 SHA、远端 SHA、隐私文件和用户在推送期间的新修改。

## 本轮需求

（把用户当前需求完整粘贴在这里）
```

## 十、这套方法的最终判断标准

一套面向 AI 编程流程是否高效，不看 AI 写了多少文字，而看以下结果：

- 用户是否只需说明需求和验收结果；
- AI 是否能独立完成研究、实现、测试、构建和文档；
- 每一轮是否都有清楚的目标、成品和通过标准；
- 已通过功能是否稳定保留；
- 失败是否能根据证据快速定位；
- 新 AI 是否只读少量当前文档就能继续工作；
- 项目是否随迭代越来越清楚，而不是文档和代码越来越混乱。

最简洁的总结是：

> 用户掌握产品方向和真实体验，AI 承担工程实现和质量证明；双方通过编号验收结果快速闭环，最终用当前文档与 Git 版本保存可靠成果。
