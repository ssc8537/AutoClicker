# 自动连招：唯一阶段路线

本文件是项目唯一总路线，也是每一位 AI 必须持续原地更新的阶段脚本。禁止在根目录新建第二份路线、总表或阶段计划。每轮按“实现 → 自动检查 → Windows 人工验收 → 归档”推进；完成全部阶段后，才可称为程序完成。优秀案例只用于核对用户需要的行为、命名和紧凑布局，不复制品牌、资源、C++、JSON/QIM 或无关功能。

| 阶段 | 目标 | 当前状态 | 完成标准 |
|---|---|---|---|
| 1. 安全宏底座 | Python `run(player)`、可中断等待、键鼠发送、停止与异常释放 | 已完成 | 自动测试与安全窗口输入通过。 |
| 2. 宏库 | 新建、编辑、校验、保存、改名、删除、AI 提示词入口 | 已完成 | 用户宏不丢失，错误宏不执行。 |
| 3. 触发与多宏 | 每宏一个键；同键可并发多个宏；按下/切换；次数/速度/状态即时保存 | 用户接受通过；英文完全正确，中文轻微问题作为已知 bug 保留 | 保存后第一次按压即生效；英文按下/切换正确；中文限制不再阻塞开发。 |
| 4. 桌面与视觉 | 中文无黑窗 EXE、窗口/托盘、用户角色图标、粉橙 UI、DPI、启动前置 | 已 Windows 验收通过 | 2560×1440、133% 清晰，底部不裁切。 |
| 5. 完整单键自定义 | 全局键、宏键和共享动作支持标准键盘键与五鼠标键 | 已 Windows 验收通过 | 默认 `· / \``，F2/F12 无固定功能，冲突安全。 |
| 6. 共享动作与声音 | 动作名称/按键可改、同键复用；首次选中与全局启停提示音 | 已 Windows 验收通过 | 自己发送的输入不能再次触发宏/声音；日志能定位每次真实提示音。 |
| 7. 功能页 | 一排快捷连点；共享游戏键位移到功能页；设置页提供 OSD 开关 | 用户 Windows 验收通过 | 一排布局；按下/切换正确；1–10000ms；退出释放。 |
| 8. AI 作者提示词 | 覆盖真实 API、精确键值、三角色技能档案、战斗逻辑、元数据和安全边界 | 用户六步验收通过 | 陌生 AI 只读提示词即可输出完整可保存脚本；未知角色事实不猜测。 |
| 9. 稳定性与发布 | 全局 hook 真实验证、单实例、CPU/残留复核、EXE 构建和 GitHub 回退版本 | 已完成并发布 `VV023-accepted-baseline` | 只运行一个实例；退出无进程/输入残留；普通推送。 |
| 10. 阅读、图标与性能优化 | Markdown 非模态提示词、饱满统一图标、启动/运行效率优化 | 用户四步验收全部通过 | 提示词可放旁边阅读；图标透明边距减少；安全启动更顺畅。 |
| 11. 角色连招转换 | 将用户 QuickInput JSON 等价转换为本项目 Python 宏 | 用户四步验收全部通过 | 1 秧秧、2 琳奈、3 千咲；动作次数和等待保持可核对。 |
| 12. OSD 与游戏输入兼容性 | 长名称完整显示；OSD 不抢焦点；管理员 EXE；SendInput 失败诊断 | 用户三步 Windows/游戏验收全部通过 | 长名称不裁切；游戏前台能触发并接收物理键；失败时日志有证据。 |
| 13. 毕业发布与交付 | 固化验收版本、版本标签、GitHub 回退点和清晰交接 | 已发布到 GitHub 默认主干 `master`；`VV024-graduation` 指向 `b209425` | 提交与标签可回退；远端 SHA 已核验；不添加无需求功能。 |
| 14. 批量旧宏迁移 | 将用户提供的 9 个 QuickInput JSON 只读转换为 Python 宏 | 当前保留 8 个可运行转换宏；用户已删除图像识别占位并自行选择启用状态 | 动作顺序、显式等待和固定循环可核对；不伪造图像识别；发布时保留用户当前状态。 |
| 15. 双主题与双布局美化 | 保留经典粉红/顶部标签；新增头像樱空侧栏；修复状态色、居中与全向拉伸 | 用户验收通过并发布 `VV025-ui-gallery-release`（`912b873`） | 主题/布局独立；侧栏窄且居中；序号居中；八方向缩放；淡蓝选中不覆盖文字色。 |
| 16. 启动默认启用 | 每次打开程序后全局自动化直接可用，不再要求先按全局开关键 | 用户验收通过并发布 `VV026-startup-enabled`（`685d85f`） | 启动显示并实际处于已启用；启动不自动执行宏；第一下全局键禁用、第二下恢复。 |
| 17. 游戏侧键与日志管理 | 修复后台窗口矩形误吞游戏侧键；日志按日期归档；三档清理与手动浏览 | 用户步骤 1–5 全部验收通过 | 游戏前台侧键产生启动日志与 OSD；每天独立目录；可按 30/5/1 天清理且不删今天；可打开文件夹手动管理。 |
| 19. 原生开发连招工作台 | 零OBS依赖；项目内WGC + GPU/CPU可选编码回放；按键时间线；外挂字幕、双音轨与历史 | 全部功能及19D历史浏览均已由用户验收通过；最终交付已固化到`master` | 一份原始视频加外挂字幕；独立双音轨；全局按键日志；历史浏览与安全回收站删除；源码、构建说明和许可完整。 |

## 与优秀案例 1 的差异结论

| 分类 | 案例能力 | 本项目结论 |
|---|---|---|
| 已对齐 | 宏库、单键触发、按下/切换、次数/速度、即时保存、快捷连点、全局启停、托盘、状态提示 | 已按可信 Python 宏架构实现，不复制案例格式。 |
| 明确不要 | 坐标/轨迹、滚轮、游戏内存/游戏注入、OBS/成品录屏程序、定时无人操作、OCR/图像/颜色识别、手柄、变量流程、按键/鼠标屏蔽、JSON/QIM | 永久排除；Stage 19 只允许项目内原生回放和用户本人允许键的旁路日志。 |
| 非毕业必需 | 宏分组、导入/导出、开机启动、多语言、在线更新 | 当前不实现；Python 宏本身可直接复制备份，继续加入只会扩大维护面。 |

源码对照依据：案例 `source/ui/FuncUi.*` 的快捷连点、`TriggerUi.*` 的触发扩展、`MacroUi.*` 的录制/导入导出，以及 `source/src/action.h` 的完整动作类型。当前产品只保留用户明确需要且符合安全边界的子集。

## 毕业标准

当前产品范围已完成至Stage 19，完成标准如下：

1. Stage 7–9 已验收事实归档并发布 VV023 回退基线。
2. Stage 10 Markdown 窗口、图标和性能优化通过 Windows 验收。
3. Stage 11 的秧秧/琳奈/千咲 Python 宏通过用户动作核对。
4. 自动测试、编译、正式无黑窗 EXE 构建通过；用户要求时再发布新版本。
5. Stage 12 的长名称 OSD 与管理员游戏输入已通过 Windows/游戏验收。
6. Stage 14 的 8 个可运行宏通过用户安全抽样；依赖图像识别的 1 个文件保持禁用并明确说明限制。
7. Stage 15 的历史双主题版本已经发布；用户后续明确收束为唯一樱空花园/画册侧栏，当前源码和README均以最新需求为准。
8. Stage 19的原生回放、全局按键记录、按键窗口、外挂字幕、桌面/麦克风独立音轨、声音预检和历史浏览均经用户逐轮验收。
9. Python/Rust自动回归、正式管理员便携包、第三方许可、构建说明和发布包隐私检查全部通过；GitHub主干使用普通推送，不强推。

## 永久产品边界

1. 每个宏对应一个脚本和一个触发键；同一个触发键可绑定多个宏并发运行。
2. 不做两个键共同触发一个宏。
3. 只做键盘连招、鼠标左/右/中/侧键点击或按住、快捷连点和用户明确要求的桌面 UI。
4. 除 Stage 19 的项目内原生回放与物理按键日志外，不做坐标轨迹、滚轮自动化、图像/OCR、角色状态识别、游戏内存/游戏注入、OBS、手柄、案例 JSON/QIM 或无关页面。
5. 用户的 `macros/`、`config/` 不得被测试执行、覆盖或清理。

## 已接受的已知问题

- 中文输入法下 `hello_world/goodbye` 的物理字母会进入 IME 组合态；用户确认英文输入法完全正确，并接受中文触发仍可能有轻微偶发现象。该问题不再阻塞后续开发。
- 若未来重新处理，必须遵守根 `AGENTS.md` 的证据边界：先取得真实 hook 边沿日志，禁止继续猜改状态机。

## 维护规则

1. 每次实现完成、用户验收或范围改变，都直接更新本文件的状态和“当前唯一下一步”。
2. 根目录不再保留 `PROJECT_OUTLINE.md`、`STAGE_TEST_PLAN.md`、`PROJECT_STRUCTURE.md` 或旧宪法索引；Git 历史可恢复它们。
3. 旧阶段规格和验收教程已由 `PRODUCT_REQUIREMENTS.md` 取代；当前工作区不再保留它们。

## 当前唯一下一步

Stage 19及19D均已由用户验收通过，最终交付版本已按用户授权以单一发布提交正常推送到GitHub`master`；准确提交SHA以`git log -1`和远端`refs/heads/master`为准。当前没有自动开始的下一阶段，等待用户提出新的明确范围。仍禁止强推、自动扩展功能或删除用户宏/配置。
