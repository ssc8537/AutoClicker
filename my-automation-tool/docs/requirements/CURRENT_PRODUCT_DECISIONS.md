# 当前产品决定（优先级最高）

## 2026-07-18：Stage 2D Windows 验收已通过；Stage 3K 自动检查完成、待 Windows 验收

Stage 3K Windows 教程中，用户确认原第 2–6 步通过；第 1 步发现共享键位表被布局压缩而不可见。该反馈已在同一 Stage 3K READY 范围修复：设置页整体只允许纵向滚动，八项字段、保存按钮和提示保持自然高度；不改变 INI、键位校验或语义 API。宏库已新增确认后回收站删除：活动宏先停止、停用和清空路径，失败不永久删除。已启用宏可编辑、保存和改名；当前运行使用 F9 启动时快照，新文件只在下次 F9 reload 生效，改名同步唯一活动路径。多宏并行仍为独立后续阶段。

宏页右栏“编辑”下方的“AI 提示词”始终可用；它只打开中文只读窗口并复制文本。Stage 2C、Stage 2C-R、Stage 2C-S 与 Stage 2C-T 已获用户通过。提示词仍只允许 `player.tap(key, hold_ms=20)` 与 `player.sleep(ms)`，有效选中宏可附带已保存源码，绝不执行源码。E 是战技，Q 是声骸技能，R 是大招；鼠标、角色切换/技能语义 API、键位配置、F9/F12/F2 改动、图像识别与案例角色 AI 均未发布且禁止写入模板。

用户已确认 Stage 2C-T 的 Windows 教程 1–6 与 Stage 2C-U 的教程 1–4 全部通过。Stage 2C-U 只修订作者文字：全部六个不同角色方向的普通切人前至少等待 50ms；A→B 后立即 B→A 的回切从切到 B 起至少等待 1080ms（1000ms 冷却 + 80ms 安全余量）；`player.tap("R")` 后立刻 `player.sleep(1500)`，它覆盖随后普通与回切门槛，结束后可直接发送其他 `1`/`2`/`3` 数字键。这仍不表示动画/CD/当前角色检测、角色语义 API 或自动输入逻辑。默认模板只有用户明确要求时才由 AI 更新；用户维护的本地提示词文字不能新增程序能力。

Stage 2D 只修订触发页表格视觉：新增连续“序号”列；宏名称显示去 `.py` 的主体；所有表头和单元格居中。显示变化绝不改变磁盘文件名、宏解析、选择路径、唯一启用/停用、无效红字、右侧只读详情、F9/F12/F2、OSD、fail-closed、宏执行或真实输入。该页仍只有状态列可启用/停用有效宏。

Stage 2D 触发页的“序号、去 `.py`、全列居中”已由用户验收。Stage 3K 已实现共享 `config/game_keybinds.ini`、设置页八项键盘映射和 `player.切换(1|2|3)`、`player.战技()`、`player.声骸()`、`player.大招()`、`player.跳跃()`、`player.处决()`：默认键是 `1/2/3/E/Q/R/Space/F`，拒绝不支持、重复及 F2/F9/F12。语义 API 仅从已启用宏的 F9 路径发送配置物理键，沿用 `tap` 的可中断释放路径，不识别游戏状态。Stage 3M 和 Stage 4T 仍是独立后续阶段，未 READY。

本文件用于覆盖早期文档中与当前方向冲突的长期设想。冲突时按“用户最新明确指令 → 本文件 → 最新交接 → 旧规格/旧任务”处理。

1. Python 是唯一运行宏格式；JSON/QIM 是历史材料，不进入运行路径。
2. 当前有效热键仅 F9、F12；F2 仅 UI 占位。
3. 当前只允许 `switch/down`；不恢复 `up` 或 `press`。
4. 两份优秀案例只读参考且不提交；只借鉴 Quickinput UI/触发框架和 ok-ww 输入小接口思想。
5. 图片 5 关于/更新页明确舍弃。
6. v021 在 RequirementCertifier 输出 READY 前，仅完善需求文档，不写 UI 代码。（历史门槛，已完成。）
7. V023 已获受限实施 READY：只可在 `main.py` 的 `MainWindow._setup_ui()` 与四个 `_build_*_page()` 中调整布局、文案、间距与 Candy 样式；可同步更新离屏 UI 测试断言。不得修改窗口 642 固定宽度/510 最小高、F9/F12/F2、OSD、播放器、关闭清理、真实输入、数据格式或任何候选扩展。
8. 用户已通过 V023 的 Windows 外观、F9/F12/F2 安全回归与触发页中文显示；“模式 / 切换 / 按住 / 次数 / 速度”不改变内部枚举、宏数据或热键行为。本次明确保留未提交、未推送状态。
9. GitHub 发布默认关闭：没有用户针对当前版本的人类验收通过，不创建提交、不推送。用户明确要求发布时可例外；任何发布都必须实时预检、普通快进推送并禁止 `--force`。
10. 专业实现、案例行为和技术取舍优先由 KnowledgeExpert 根据项目资料、优秀案例 1 与优秀案例 2 的源码证据回答；只有证据与保守默认均不能决定且会改变用户目标、安全、兼容性或验收时，才询问用户。真实 Windows 视觉、真实输入安全和最终人工验收仍由用户完成。
11. 用户要求 C1 以自动文件检测替代手动刷新；静态校验仍不执行宏顶层代码，只有 F9 可加载活动宏。校验结果不再作为 UI 列或错误详情显示：无效宏仅以脚本名红字表示，且不可启用。编辑、保存、重命名、创建宏、多宏并发、每宏热键、鼠标、窗口、音效、录制和定时均不在范围内。
12. 新最高产品方向：以优秀案例 1 的源码事实作为技术与 UI 参考，只实现 MyAutoPlayer 已确认的子集；禁止复制案例源码、资源、品牌、JSON/QIM 格式或未授权能力。可信本地 Python 是唯一宏语言，宏保持 Python 元数据与 `run(player)` 接口。本条优先于早期 JSON、多功能和过度员工调度的历史文字。
13. Stage 1 宏页按优秀案例 1 的布局层级适配为：左侧宽白色 Python 脚本名单（行号、名称）与右侧窄的纵向操作栏；保持 Candy 粉红主题。不得显示校验、错误摘要或错误详情；无效宏仅脚本名称红字。宏页只负责选择脚本，右栏仅保留名称只读框与编辑、保存、重新加载、删除等禁用占位，绝不改变可触发状态。触发页是唯一启用/禁用入口：列出全部发现的脚本，固定“名称、按键、模式、状态”四列；有效项显示 F9 与模式，点击状态列启用/禁用唯一活动宏，无效项显示红字和“不可启用”。右侧配置栏只读显示 F9、模式、次数、速度和状态。不得改变 F9/F12、F2 占位、OSD、fail-closed 或 Python 宏接口，亦不得加入 JSON/QIM、录制、窗口、鼠标、多宏、导入导出或真实编辑保存。
14. 用户已批准鼠标宏分阶段路线，且 RequirementCertifier 已对 Stage 2A 给出 READY：真实 Python 宏的新建、重命名、内置编辑、原子保存与重新加载。Stage 2A 不包含导入、导出、删除、触发配置、鼠标 API、录制或多宏。鼠标左/右键按下、释放、单击和有限连点仅在后续 Stage 3M 实现，并且只能由触发页已启用宏的 `run(player)` 调用；未调用鼠标 API 的宏不得影响正常鼠标。
15. 用户已通过 Stage 2A Windows 验收并明确授权 Stage 2B 编辑体验：宏页文件列表显示 stem、名称框 Enter 复用安全重命名、弹窗按钮中文、Python 高亮和 Tab 四空格缩进。此条不扩大运行期 API；固定 F9/F12、F2 占位、唯一活动宏、AST 静态校验、原子保存、OSD 与 fail-closed 不变。全局游戏键位 INI、键盘语义 API、鼠标 API 与作者文档是后续分阶段计划，均未 READY。
