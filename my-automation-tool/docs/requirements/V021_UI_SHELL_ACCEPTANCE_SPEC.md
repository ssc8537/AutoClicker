# v021：四页 UI 外壳验收规格（需求完善中）

> 需求信心审查：**NOT READY**。本文件在所有未决项确认前禁止 UI 代码实现。

## 已锁定

- 页面：宏库、触发、功能、设置；视觉参考为 `docs/reference/quickinput-ui/` 的图片 1–4。
- 风格：Quickinput 红库方向；图片 5 关于/更新页不采用。
- 运行边界：F9、F12、现有 OSD 必须不回归；F2 仅显示预留位置，不注册、不响应、不显示 OSD。
- 功能边界：本轮不做鼠标、窗口、录制、音效、定时、OCR、多脚本、任意热键或 Python 宏编辑写回。
- 功能页：仅视觉外壳，不产生任何运行行为。
- 窗口规则严格参考 Quickinput：默认 `642×510`；宽度固定为 `642`；最小高度 `510`；最大高度为当前主屏高度；只允许垂直缩放；启动时恢复已保存的有效尺寸并居中，否则使用默认尺寸。源码证据：`优秀案例1-Quickinput/Quickinput-main/source/ui/MainUi.ui:5-29`、`MainUi.cpp:10-18,152-155`、`source/src/type.cpp:51-52,223`。
- 页面导航可以切换，默认页为“宏库”（索引 0）；本阶段不保存/恢复最后打开页。证据：`MainUi.h:25`、`MainUi.ui:281-311`、`MainUi.cpp:21-34`。
- 宏库只读展示唯一的 `hello_world.py`；新建、编辑、保存、刷新、双击和右键菜单均不产生行为。
- 触发页只读展示 F9、`MODE`、`COUNT`、`SPEED`；不写回 Python 文件、不改变运行配置。
- 设置页只展示既有 F12/OSD 状态与说明，不提供鼠标点击切换全局状态。
- 功能页可绘制“快速鼠标点击开关”等原版外观控件，但全部灰置并标注“后续阶段”；不注册热键、不执行点击、不修改任何设置。

## 待用户确认（阻塞 READY）

1. “参考图一致”的验收尺度：直接按截图近似，还是采用明确的红色色值、字体、间距和字号。
2. 宏库和触发页的只读展示控件是否允许“仅选中行/切换页面”的无副作用交互，还是所有控件均灰置。
3. UI 自动测试采用现有 `unittest` 加 PySide6，还是引入 `pytest-qt`；截图验收的通过标准。

## 重新审查条件

以上三项均得到明确答案后，RequirementCertifier 才能重新评估 READY。通过后，开发者只可实现已确认的 UI 外壳范围。
