# v021：四页 UI 外壳验收规格（需求完善中）

> 需求信心审查：**NOT READY**。本文件在所有未决项确认前禁止 UI 代码实现。

## 已锁定

- 页面：宏库、触发、功能、设置；视觉参考为 `docs/reference/quickinput-ui/` 的图片 1–4。
- 风格：Quickinput 红库方向；图片 5 关于/更新页不采用。
- 运行边界：F9、F12、现有 OSD 必须不回归；F2 仅显示预留位置，不注册、不响应、不显示 OSD。
- 功能边界：本轮不做鼠标、窗口、录制、音效、定时、OCR、多脚本、任意热键或 Python 宏编辑写回。
- 功能页：仅视觉外壳，不产生任何运行行为。

## 待用户确认（阻塞 READY）

1. 窗口默认尺寸、最小尺寸、是否允许缩放，以及页面导航的位置和默认页。
2. “参考图一致”的验收尺度：直接按截图近似、还是采用明确的红色色值、字体、间距和字号。
3. 宏库页是否展示唯一 `hello_world.py`；列表、分组、刷新、新建、双击、右键分别是可用、灰置、隐藏，还是显示“后续阶段”。
4. 触发页的 `MODE/COUNT/SPEED` 是只读展示，还是允许编辑并保存回 Python 文件。若允许保存，即不再是纯 UI 外壳，必须单独规划。
5. 设置页连接 F12/OSD 的方式：只展示说明和状态，还是允许点击按钮切换现有全局状态。
6. 功能页所有未实现控件统一采用哪种表现：灰置、灰置加“后续阶段”标签、隐藏，或点击后提示。
7. UI 自动测试使用现有 `unittest` 加 PySide6，还是引入 `pytest-qt`；截图验收的通过标准。

## 重新审查条件

以上七项均得到明确答案后，RequirementCertifier 才能重新评估 READY。通过后，开发者只可实现已确认的 UI 外壳范围。
