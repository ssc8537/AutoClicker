# C1 `main.py` 拆分报告

## 触发条件

自动检测扩展完成后，`main.py` 达到 561 行，且同时承担窗口组装、热键/播放器协调、宏库表格、静态扫描调用、文件监听、防抖、活动状态与失效处理，职责混杂，超过项目 500 行检查门槛。

## 结论

RequirementCertifier 已对本次受限重构给出 `READY`。新增 `src/ui/macro_library_panel.py`，只承载宏库页面、监听、150ms 防抖、静态列表与活动状态信号；`main.py` 保留 F9/F12、播放器、OSD、窗口与失效后停止/`mark_finished("f9")` 协调。此拆分不增加产品功能。

## 不变边界

- 扫描和监听不执行宏源码；仅 F9 的运行时重载可加载宏。
- 仍只允许一个活动宏；失效时停止、清空且 F9 fail-closed。
- 不新增编辑、保存、创建、多宏、额外热键、鼠标、窗口、音效、录制、定时、JSON/QIM 或发布。
