# Stage 2D `main.py` 文件长度拆分报告

日期：2026-07-18  
审查者：AntiHallucination + 项目负责人  
结论：本轮不拆分；后续如获独立重构 READY 再处理

## 事实

`main.py` 当前为 545 行，超过 500 行阈值。Stage 2D 只改动触发页表格的列数、显示文本、对齐和状态列索引，未新增运行时能力。

## 职责与拆分评估

该文件现同时包含应用启动、主窗口、四页 UI 构建、触发页投影和热键协调，长期看存在职责混杂。稳定入口必须保留为 `main.py`；可行的后续拆分方向是将触发页投影和表格渲染放入 `src/ui/`，并由 `main.py` 保留窗口装配入口。

本轮不能拆分：RequirementCertifier 的 Stage 2D READY 仅授权触发表格视觉，未授权重构、模块移动或公开/内部调用关系调整。拆分将扩大回归面，不能作为本阶段的附带修改。

## 风险与后续门槛

当前风险是未来继续向 `main.py` 堆叠 UI 职责。若用户决定进行代码清理，必须先取得独立 RequirementCertifier READY，明确稳定入口、模块边界、迁移测试和回退方案；未获 READY 前不得拆分。
