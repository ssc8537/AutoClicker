# 里程碑记录

## v020：团队与 UI 规格建立

- 做了什么：创建角色规范、项目入口 README、结构文档、案例知识索引、Quickinput 按钮级 UI 规格和四张项目自有参考图副本。
- 质量：规格来自 Quickinput 源码审查，不以截图猜测控件功能；两份案例仍保持 Git 忽略。
- 测试：在 v019 基线创建前运行 16 项单元测试与 Python 编译，全部通过。
- 遗留：四页 UI 还没有实现；F2 只预留，不注册热键。
- 下一步：仅实现四页 UI 外壳和红库主题，保持 F9/F12/OSD 现有运行行为不变。

## v020.1：需求信心门槛

- 做了什么：新增 RequirementCertifier、仓库级 `AGENTS.md` 最高指令和可审计的 READY/NOT READY 清单。
- 质量：每位负责人接手、每个新阶段和每次运行代码变更前都必须先审查；无 READY 不写代码。
- 下一步：对四页 UI 外壳任务先做 RequirementCertifier 审查，必要时只补规格文档。

## v020.2：UI 需求证据与完整交接

- 做了什么：KnowledgeExpert 从 Quickinput 源码确认 642×510、固定宽度、仅纵向缩放和默认宏库页；用户确认未开发控件只做灰置占位。
- 质量：事实已同步到 v021 规格和完整交接；UI 任务仍保持 NOT READY，没有写运行代码。
- Git：提交 `06bef74` 已于 v020.3 开始前快进推送到 GitHub。

## v020.3：项目负责人和团队协作闭环

- 做了什么：新增唯一“项目负责人”配置；明确 12 个角色的唯一职责、证据优先级、少问用户原则、两级需求审查、Git/测试/交接顺序和文件长度门槛。
- 模型决定：项目负责人和全部员工永久统一为 `GPT-5.6 Terra 高 / high`；仓库配置不能伪称能够覆盖平台实际分配。
- 交接：稳定入口保留为 `CURRENT_HANDOVER.md`，历史标题使用递增数字；本轮编号为 `2-项目负责人提示词与团队流程审查`。
- 防幻觉：初稿检查 9 个文件；提交前终检覆盖 14 个新增/大改文件，最长 192 行，项目负责人配置 154 行。全部职责单一且低于 500 行，不需要拆分。
- 测试：16 项单元测试、Python 编译和 `git diff --check` 全部通过；未启动 GUI、未发送真实输入。
- 遗留：v021 四页 UI 外壳仍需独立 RequirementCertifier 实施前终审，本轮没有授权或实现 UI。

## v021：四页 UI 外壳

- 做了什么：在既有 `MainWindow._setup_ui()` 内制作 Candy 粉红四页外壳，包含宏库、触发、功能和设置；宏库/触发只读，功能页禁用，设置页显示既有全局状态。
- 安全边界：没有改动 F9/F12 注册、播放器、OSD、关闭清理或真实输入路径；F2 只显示为禁用占位。
- 自动验证：原有 16 项回归加 7 项 UI/热键注册/退出清理测试，共 23/23 通过；Python 编译和离屏截图检查通过。
- 人工验收：用户已确认 F9/F12/F2 与安全回归通过，并认可 Candy 粉红方向；整体视觉仅部分认可。下一步是 V022 案例视觉与窗口规则对齐审查，不直接编码。
