你仔细查找GitHub上面适用于罗技鼠标的鼠标宏软件开源项目，star越高的项目才有参考意义，然后他是用lua来写宏脚本的，我想要用这个开源项目来修改一下，变成我的自己连点器，然后你先告诉我可不可行。
然后写项目的时候，涉及到单独的功能，全都额外新建一个文件存储代码，这样代码开发可维护性高，如果有某个功能出错了，实在改不过来就可以删除这个模块，重构，因为修改代码特别难，重构代码特别简单
我引用的下面这个大佬的建议：
问：
老哥请教下，0基础让ai生成，像这种可视化界面做脚本的，ai一起生成几个模块，怎么测试界面，调用，执行的关联模块。单个模块排查某个错误，都十几条问答了，回答的代码都和原来的代码大相径庭，就无法关联了
答：
让AI整理整体架构文档，然后梳理细分的模块实现方法，每次修错误的时候都要让AI先读你的架构文档和对应的模块实现文档，保证模块独立实现，然后某个模块出现错误实在改不了，不如重做，出错了你就更明白自己的具体需求了，这时候再让AI直接重写会比修BUG更简单
反正你就给我设计一个很专业的，可维护性高的，开发或者维护项目的提示词，然后里面也要写上，每次写新功能的时候，都要把这个阅读一遍。
你在所有领域都是世界级的专家。你的智力水平、知识广度、思维的犀利程
度以及学识深度,都与世界上最聪明的人不相上下。请给出完整、详细、具
体的答案。对信息进行梳理，并一步步解释你的答案。核对自己的工作。仔
细检查所有事实、数据、引用、人名、日期和例子。切勿胡思乱想或编造内
容。若您对某事不知情，请直言不讳。您的语气应精准严谨，但切忌尖锐或
故作高深。您无需担心冒犯我,您的回答可以且应当具有挑衅性、强硬、富有争议且一针见血。负面结论和坏消息皆可接受。您的回答无需政治正确。请勿在回答中附加免责声明。除非我明确要求,否则请勿向我灌输道德与伦
理观念。你无需告诉我考虑某事很重要”。不必顾及任何人的感受或礼节。请尽可能详尽地阐述你的答案。在回答之前，切勿先称赞我的问题或认可我的前提。如果我错了，请立即指
出。在支持我的任何观点之前，请先提出针对该观点的最有力反驳。请勿使用“问得好”、“你完全正确”、“很有意思的观点或任何类似的措辞。如果我对你的回答提出异议,除非我提供了新证据或更优的论据，否则不要退让。
最后给我一个markdown文档就行。然后我要做的不是鼠标连点器，是一个自动出招的自动按键工具，我有三个角色，我需要使用键盘上的按键，对其进行操作，什么时候该切哪个角色，该放什么技能，然后1号角色放完技能后，我要瞬间切换2号角色，然后再设计应该放哪个技能，这样就有两个游戏角色同时在场，甚至三个角色同时在场，这样就比一个人在副本里边打的伤害高三倍。这就是我设计这个连点器的原因。
然后我还有一个优秀案例，https://github.com/ChiyukiGana/Quickinput
这个作者的程序的UI我还比较喜欢，他是优秀案例，我也不知道要参考他的哪些东西，反正我会把它拉去到我的项目文件里面，命名为-优秀案例1-quickinput，然后你就也要写出专业级别的项目阅读提示词，让AI到时候仔细阅读我的优秀案例，取他的优秀经验，转化为帮助我开发的好方法。
然后，我的鼠标是普通鼠标，你要帮我思考一下逻辑。鼠标它的代码写出来了之后，这个鼠标宏连点器自动按键工具能否用在普通鼠标身上。所以首先我要完成的任务，就是让这个自动按键工具能够在普通的鼠标上正常运行，运行成功的例子就是我的普通鼠标按下一个快捷键，键盘上就出现了Hello World这一行英文是键盘打出来的。然后，我的优秀案例，它其实也能够做到Lua的代码，相当于快速轮切。我目前就在用这个优秀案例帮助我打游戏。但是他编写脚本我感觉比较墨迹，它是一行一行的编写出来之后，好像是Jason文本。然后，我又不能写代码，如果我能写代码的话，逻辑这些就能够控制得很好，因为我会给出这个角色的出招表，然后，每个按键之间的间隔，我按得特别慢，那么它们的间隔可能就是两秒。然后，我需要如果我能够写代码的话，那么这些间隔我就可以自定义地调整这些按键的间隔。比如说2倍速、1倍速或者什么的，或者说给所有的时间都统一剪短一点点的时间，这样就方便我整体修改代码，而不是说他的Jason每一次只能修改一个按键的状态，特别的麻烦
然后我完全不需要什么压枪红什么的，我不玩枪战游戏嗯，我只需要快速轮切能够自动实现的按键工具就行了。
然后，还有一个优秀案例，它里面的代码有一些是能够操控角色实现按键的。然后我会把它拉取到我的项目里面，叫做优秀案例2-okww。它里面有一个能够控制角色放技能的代码，然后你也要写一个提示词儿。参考他的代码风格、项目文件，吸取他的优秀经验，把这些经验转化为我的项目的好帮手然后我给你一点它的代码，你可以参考一下它的代码，找到它的源代码，写出这个代码的位置。
然后我想要的就是一个程序里面可以让我自己新建一个脚本，写出来了这个脚本之后，我可以绑定一个快捷键，按下了我就让它瞬间触发。然后写这个脚本所使用的语言可以用Lua也可以用普通的Python代码语言，我只需要那个框架就行了，还有写脚本的各种说明方法那些，到时候，脚本我会自己去写的，我会跟对根据每一个角色的案件情况设置他们的代码，每个角色的代码都是让我来书写的，这个你不用操心，你只需要考虑这个脚本怎么绑定到快捷键上面就行了。还有一个特别重要的需求。在我的优秀案例1里面，他写了一个逻辑，就是程序打开的时候，我的绑定的快捷键是不会触发我的自动按键脚本的。只有当我的鼠标不在我的自动连点器的程序的时候，它才可以使用绑定的按键触发对应的脚本。
可配置序列：你需要一个配置文件或UI来自由编辑每个角色的技能、顺序、延迟时间
按下鼠标的上侧键，或者鼠标上的任意一个键，或者键盘上面的任意一个键，就可以执行我绑定的代码。
然后，你不要着急先帮我完成任务。
下面就是我的优秀案例2里面的某个角色的他的自动出招的代码，我想要吸取一下他是怎么写代码的，我想要学习，所以你就要把提示词写出来，让机器人去阅读。优秀案例2里面的对应这个代码的源代码，学习它的思想，然后辅助我就是用他的格式框架，这些或者是代码某些函数就是用它的，因为我自己开发可能比较难，我就借鉴一下别人的内容吧，开发出一个自己的，这样才是最好的。下面我就把那个代码发给你，这就是优秀案例2号某个角色的代码
```python
import time, cv2
import numpy as np
from src.char.BaseChar import BaseChar, SwitchPriority, forte_white_color


class Cartethyia(BaseChar):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self.is_cartethyia = True
        self.buffs = {'sword1': None, 'sword2': None, 'sword3': None}
        self.template_shape = None
        self.try_mid_air_attack_once = False
        self.transform = False
        self.res_time = -1
        self.n4_time = -1
        self.init_template()

    @property
    def intro_motion_freeze_duration(self):
        return 0.6 if self.is_cartethyia else 0.78

    @intro_motion_freeze_duration.setter
    def intro_motion_freeze_duration(self, _):
        pass

    def init_template(self):
        self.template_shape = self.task.frame.shape[:2]
        template = self.task.get_feature_by_name('forte_cartethyia_sword3')
        original_mat = template.mat
        h = original_mat.shape[0]
        self.sword3_half_mat = original_mat[:int(h * 0.5)]
        target_box = self.task.get_box_by_name('forte_cartethyia_sword3')
        target_box.height = int(h * 0.6)
        self.sword3_half_box = target_box

    def on_combat_end(self, chars):
        if not self.is_cartethyia:
            next_char = str((self.index + 1) % len(chars) + 1)
            self.logger.debug(f'on_combat_end {self.index} switch next char: {next_char}')
            start = time.time()
            while time.time() - start < 6:
                self.task.load_chars()
                current_char = self.task.get_current_char(raise_exception=False)
                if not isinstance(current_char, type(self)):
                    break
                else:
                    self.task.send_key(next_char)
                self.sleep(0.2, False)
            self.logger.debug(f'on_combat_end {self.index} switch end')

    def get_switch_priority(self, current_char=None, has_intro=False, target_low_con=False):
        if not self.is_cartethyia:
            return SwitchPriority.MUST
        return super().get_switch_priority(current_char, has_intro, target_low_con)

    def do_perform(self):
        self.transform = False
        if self.has_intro:
            self.continues_normal_attack(1.2)
        else:
            self.click_echo(time_out=0)
        if self.is_small():
            self.logger.info(f'is cartethyia')
            self.wait_down()
            if self.acquire_missing_buffs():
                return self.switch_next_char()
            self.check_combat()
            self.try_mid_air_attack()
            self.check_combat()
            if self.click_liberation():
                self.is_cartethyia = False
                self.last_res = -1
                self.transform = True
            elif not self.is_small():
                self.transform = True
        else:
            self.logger.info(f'is fleurdelys')
        if self.click_resonance_with_lib_big():
            pass
        else:
            time_out = 1.1 if self.is_small() else self.fleurdelys_n4_duration()
            start = time.time()
            while time.time() - start < time_out:
                if self.try_lib_big():
                    return self.switch_next_char()
                self.click_with_interval()
                self.check_combat()
                self.task.next_frame()
            self.n4_time = time.time()
        self.try_lib_big()
        self.switch_next_char()

    def fleurdelys_n4_duration(self):
        if not self.transform and self.has_intro:
            duration = 3.9 - (time.time() - self.last_perform)
        elif self.transform or self.is_first_engage() or \
                self.time_elapsed_accounting_for_freeze(self.n4_time, intro_motion_freeze=True) < 1.5:
            duration = 3.25
        elif (backswing := self.time_elapsed_accounting_for_freeze(self.res_time, intro_motion_freeze=True)) < 2.5:
            duration = 2 + max(0, 1.6 - backswing)
        else:
            duration = 1.9 - (time.time() - self.last_perform)
        self.n4_time = -1
        self.res_time = -1
        self.logger.debug(f'fleurdelys_n4_duration {duration}')
        return duration

    def click_resonance_with_lib_big(self):
        if self.has_cd('resonance'):
            return False
        clicked = False
        self.logger.debug(f'click_resonance start')
        last_click = 0
        resonance_click_time = 0
        while True:
            if resonance_click_time != 0 and time.time() - resonance_click_time > 8:
                self.task.in_liberation = False
                self.logger.error(f'click_resonance too long, breaking {time.time() - resonance_click_time}')
                self.task.screenshot('click_resonance too long, breaking')
                break
            self.check_combat()
            now = time.time()
            current_resonance = self.current_resonance()
            if not self.resonance_available():
                self.logger.debug(f'click_resonance not available break')
                break
            self.logger.debug(f'click_resonance resonance_available click {current_resonance}')

            if now - last_click > 0.1:
                if current_resonance > 0 and self.resonance_available():
                    if current_resonance < 0.17 and time.time() - resonance_click_time < 2.5:
                        self.click()
                        continue
                    if resonance_click_time == 0:
                        clicked = True
                        resonance_click_time = now
                    self.send_resonance_key()
                last_click = now
            if self.try_lib_big():
                break
            self.task.next_frame()
        if clicked:
            self.record_resonance_use()
            self.res_time = time.time()
        return clicked

    def is_mid_air_attack_available(self):
        if self.is_cartethyia:
            box = self.task.box_of_screen_scaled(3840, 2160, 2298, 1997, 2361, 2022, name='inner_cartethyia_space',
                                                 hcenter=True)
            self.task.draw_boxes(box.name, box)
            if self.task.calculate_color_percentage(forte_white_color, box) > 0.15:
                cropped = box.crop_frame(self.task.frame)
                gray = cv2.cvtColor(cropped, cv2.COLOR_BGR2GRAY)
                mean_val = np.mean(gray)
                contrast_val = np.std(gray)
                self.logger.debug(f'cartethyia_space mean {mean_val} contrast {contrast_val}')
                return mean_val > 190 and contrast_val < 45

    def try_mid_air_attack(self, timeout=2):
        self.get_sword_buffs()
        if self.liberation_available() or all(self.buffs.values()) or self.try_mid_air_attack_once:
            pass
        else:
            return
        if self.is_mid_air_attack_available():
            self.logger.info('perform mid-air attack')
            start = time.time()
            while True:
                self.task.jump(after_sleep=0.1)
                if self.echo_available():
                    self.click_echo(time_out=0)
                self.task.click(after_sleep=0.1)
                if not self.is_mid_air_attack_available():
                    self.sleep(0.4)
                    break
                if time.time() - start > timeout:
                    break
                self.sleep(0.1)
        elif self.try_mid_air_attack_once:
            start = time.time()
            while time.time() - start < 0.8:
                self.task.jump(after_sleep=0.1)
                if self.echo_available():
                    self.click_echo(time_out=0)
                self.task.click(after_sleep=0.1)
        self.try_mid_air_attack_once = False

    def is_small(self):
        if self.template_shape != self.task.frame.shape[:2]:
            self.init_template()
        self.is_cartethyia = bool(self.task.find_one(template=self.sword3_half_mat,
                                                     box=self.sword3_half_box, threshold=0.5))
        return self.is_cartethyia

    def try_lib_big(self):
        if self.is_lib_big_available():
            if self.click_liberation():
                self.is_cartethyia = True
                self.click_resonance()
                return True

    def is_lib_big_available(self):
        if big := self.task.find_one('lib_cartethyia_big'):
            self.logger.debug('lib cartethyia big available {}'.format(big.confidence))
            self._liberation_available = True
            return True

    def get_sword_buffs(self):
        self.buffs = {
            'sword1': bool(self.task.find_one('forte_cartethyia_sword1', threshold=0.9)),
            'sword2': bool(self.task.find_one('forte_cartethyia_sword2', threshold=0.9)),
            'sword3': bool(self.task.find_one('forte_cartethyia_sword3', threshold=0.9)),
        }
        self.logger.debug(f"buffs {self.buffs}")
        return self.buffs

    def acquire_missing_buffs(self):
        self.get_sword_buffs()
        if all(self.buffs.values()):
            return False
        if has_perform_action := not all(self.buffs[k] for k in ['sword2', 'sword3']):
            self.logger.info('acquire missing buffs')
        if not self.buffs.get('sword2'):
            template = self.task.get_feature_by_name('forte_cartethyia_sword2')
            h = template.mat.shape[0]
            half_mat = template.mat[:int(h * 0.5)]
            half_box = self.task.get_box_by_name('forte_cartethyia_sword2')
            half_box.height = int(h * 0.6)
            time_out = 3.5
            if try_once := bool(self.task.find_one(template=half_mat, box=half_box, threshold=0.85)):
                time_out = 2 if not self.is_first_engage() else 2.5
            start = time.time()
            interrupt_handled = False
            while time.time() - start < time_out:
                if not try_once and self.task.find_one(template=half_mat, box=half_box, threshold=0.85):
                    break
                if not interrupt_handled and self.flying():
                    time_out = 2.5 if time_out == 2 else time_out
                    interrupt_handled = True
                    self.task.wait_until(lambda: not self.flying(), time_out=3)
                    start = time.time()
                self.click(interval=0.1, after_sleep=0.01)
                self.check_combat()
                self.task.next_frame()
            self.logger.debug(f'sword2: click duration {time.time() - start}')
        res = False
        if not self.buffs.get('sword3'):
            res = self.click_resonance()[0]
            self.check_combat()
        if self.liberation_available():
            res and self.sleep(0.2)
        elif has_perform_action:
            return True
        if not self.buffs.get('sword1'):
            if not has_perform_action:
                self.logger.info('acquire missing buffs')
            self.task.mouse_down()
            start = time.time()
            while time.time() - start < 1.5:
                if self.task.find_one('forte_cartethyia_sword1', threshold=0.9):
                    break
                self.task.next_frame()
            self.task.mouse_up()
            self.check_combat()
            self.logger.debug(f'sword1: heavy_att duration {time.time() - start}')
        if not any(self.buffs.values()):
            self.try_mid_air_attack_once = True
        return not self.liberation_available()
```
 Cartethyia 角色自动化代码。保持类名为 Cartethyia。保留仍然需要的 import。
在思考辅助方法、任务 API、状态、切人、冷却和战斗流程时，请使用这个 BaseChar 参考：
https://raw.githubusercontent.com/ok-oldking/ok-wuthering-waves/refs/heads/master/src/char/BaseChar.py


循环执行，你可以参考优秀案例1里面的，它可以选择按下按键就触发一次执行，或者说设定零就是无限次执行，或者说你自定义想执行多少次就执行多少次优秀案例1里面，它还有一个全局按键，可以禁用所有脚本。就是你尽管绑定了快捷键，它也不会触发。然后再次按下那个全局按键，它就会让这些脚本重新激活。等待你按下脚本的时候，它就自动执行。它是有一个全局按键的，然后这个按键也是自定义的，就是防止你的循环是无限循环，到时候你的鼠标和键盘都被控制了。
然后切换角色是123，对应1号2号3号角色。技能战技就是E，大招就是R，放声骸技能就是Q，空格是跳跃，鼠标左键是平A也就是攻击，鼠标右键就是闪避
切换后等待多久才能放技能？游戏大招可能有动画，这个我可以自己调试，这个你不用操心，我在写某个角色的脚本的时候，我会自己调试好的。
我喜欢优秀案例1的UI，我想要参考它的设计，它的美工，写出我的UI。我的程序面向是小白用户，它只会根据程序上面的按键来使用，不会其他的，也不会什么命令行。所以你的程序要写好程序界面使用什么技术站的话，你要阅读了优秀案例1和优秀案例2，你才能决定使用什么技术站。这个我也不知道，我是一个小白用户，你才是专业的机器人。然后我的需求可能也不明白，所以全部都要靠你。到时候你有什么不懂的，或者需求不好的，你在执行过程中一定要问我。这个也要写进提示词里面。
然后，我也不知道到底是找一个逻辑鼠标宏的脚本写出我的项目，还是说参考优秀案例一优秀案例二，然后参考一个优秀的逻辑鼠标宏的开源项目来制作出我的项目，还是说直接找一个项目修改一下。我也不知道哪个简单，哪个方便，哪个实现起来困难，哪个实现起来bug比较少。我也不知道，只能靠你了。然后，你在执行中的时候，你可以给我设置选择题，让我来选择哪个选项。到时候，你这些疑问，我就可以通过选择题来帮你解答。你在运行的时候，你在实际写代码的时候，一定要多多给我设置选择题，确保我能看见你的生产工作，生产环境是否可控。所有的项目实现，你都要使用专业的，尽管我是小白，但是你自己书写项目代码的时候，你一定要作为一个专业的顶尖人士
我手里面只有普通鼠标，那么放弃逻辑记号B路线，走独立应用程序的路线吧，使用优秀案例二，它的独立程序走Windows API模拟输入的路线吧。对对对，我做的就是键盘自动化序列播放器。按下快捷键→按顺序发送键盘按键（1、2、3、E、R、Q、空格、鼠标左键/右键）→带时间间隔控制。
你不需要罗技的Lua API你不需要处理鼠标移动、压枪等复杂逻辑你的核心需求是“序列播放”+“热键触发”+“循环控制”
优秀案例1（Quickinput） ：C++ + Qt开发的文本/按键快捷输入工具。值得参考的是它的UI设计和快捷键绑定逻辑，而不是底层实现。优秀案例2（ok-ww） ：Python开发的游戏自动化工具，基于图像识别和Windows API模拟输入。值得参考的是它的输入模拟方式和模块化架构。你发给我的Cartethyia代码，本质上是图像识别+条件判断+按键模拟的复杂自动化，远比你的需求复杂。你需要的是它的按键模拟部分，而不是它的图像识别和状态管理部分。
A. Python + PyQt/PySide：开发速度快，库丰富（pynput/keyboard用于输入模拟），适合快速迭代。ok-ww用的就是Python。我就使用这个技术站，速度快就行了，我首先先把程序做出来，并且没有bug，能够简单地运行Hello World这个红这个按键自动打在我的笔记本上就可以了，这个才是最重要的，我要首先就要先实现这一步，这一步完成了，才是我整个项目的所有开端，这个是最重要的。
选择Python + PyQt，因为：你和AI协作开发，Python的生态最友好ok-ww的代码可以直接参考出错了重构成本最低。
用户就使用这个语言来写脚本吧，Python：直接让用户写Python函数/类，你发的Cartethyia代码就是Python。优点是灵活，缺点是用户需要懂Python。，如果有简单的情况，也可以使用这个配置脚本，JSON/YAML配置：用户只填“按键序列+延迟”，不写代码。最简单，但灵活性最低。简单的这个Jason脚本，我就可以方便调试我的整个项目又没有成功。
程序的作用域和激活逻辑，我希望它检测鼠标。鼠鼠标不在程序窗口内，就是我的项目的程序的窗口内，我才能触发绑定按键的对应脚本。不然的话，它可能会出bug。我在程序里面绑定按键，但是这个按键绑定了那个脚本，那我输入这个按键，它就会执行脚本的内容，就会出现一系列的错乱。
按一次快捷键就执行一轮就行了，我的一轮里面写了什么代码，你就执行一次，完完整整的从头到尾执行一遍就行了。如果是无限循环，我可以自己选择每一轮之间是否有间隔，可以自己设置间隔是多少毫秒。如果正在执行脚本是否是停止还是重新开始。这里在优秀案例1里面，它有一个完整的优秀，实现它可以绑定这个按键，是按下按键就切换这个脚本的启动或者停止，或者说按下这个按键才执行这个脚本，松开就停止执行这个脚本。也可以让这个按键绑定，只有当按键松开的时候才执行这个脚本，按下的时候就停止这个脚本。它有三种模式切换，按下、松开这三种状态，用户应该可以自己选择这个按键绑定使用哪种状态来激活。
三个角色的切换逻辑切换角色也是脚本的一部分，瞬间切换2号角色指的就是延迟特别低，比如说10ms，我就切换2号角色了。然后每一个角色的技能序列就用固定顺序吧，就不要使用条件触发了。优秀案例2里面，他可能用了图像识别，识别我的角色的某种状态或者说技能CD有没有好自动选择释放，但是，我不需要这些。我只需要让他按照我的键盘上的按键去运行就可以了。我只需要固定序列就行
全局的等待延迟，全局速度我想要给每一个脚本配置一个自己的速度倍率，这个我希望在代码里面我自己给这个角色配置按键脚本的时候，它每一个技能之间都有等待延迟。我可以写代码的时候，比如说他它要有10个按键，每个按键之间都是两秒的延迟，我可以给它加上倍速，那么它的触发延迟可能就是500MS700MS，我可以等比例地放缩他们的延迟。
UI的复杂度，新建编辑，删除脚本，这些都应该实现。还有绑定快捷键，设置执行次数。脚本内容就在你的程序内，你自己开一个窗口，我就在这个程序内写代码，写代码的文件我可以自己找到，然后我也可以自己在外部编辑器书写。你也可以可视化序列编辑，拖拽式添加按键，调整延迟，然后，每个按键后面我可以写上备注，这是某某角色的，放哪个技能，我就可以自己备注好，相当于有注释
前面的可能有我的需求分析，也有我自己思考时的一些想法，还有我确定下来的一些东西，
技术成熟：Windows API模拟输入（keybd_event、SendInput）是几十年成熟的技术，Python的pynput、keyboard、pyautogui库都封装了这些底层API。
有现成参考：优秀案例2（ok-ww）已经实现了完整的键盘/鼠标模拟输入系统，你发给我的Cartethyia代码中涉及的self.task.send_key()、self.task.click()等方法，底层就是Windows API模拟输入。
需求范围可控：你只需要“固定序列播放”，不需要图像识别、状态判断、条件分支——这极大降低了复杂度。你发的Cartethyia代码有几百行，而你的核心播放器只需要几十行。---这里很重要，我写的脚本可能我需要添加重复执行，可能使用循环
第一优先级目标（Hello World验证） ：写一个最小可行程序，按下一个热键（比如F9），程序自动在记事本里打出“Hello World”。这个目标达成，整个项目的骨架就搭好了。
二、技术选型
选择：Python+PyQt/PySide对比维度Python PyQt开发速度最快调试难度低重构成本最低
输入模拟库pynput/.keyboard成熟参考代码ok-ww直接用
理由：
ok-ww就是Python写的，它的输入模拟代码你可以直接参考甚至复用你和AI协作开发，Python生态最友好，出错重构成本最低PyQt的信号/槽机制非常适合“热键触发→执行脚本”这个核心交互模型UI框架：PyQt5或PySide6（两者API几乎相同，PySide6是Qt官方支持）输入模拟库：pynput + keyboard（双保险，一个出问题换另一个）
三、项目架构设计
text
my-automation-tool/
├── main.py                    # 程序入口
├── requirements.txt           # 依赖列表
├── config/
│   └── settings.json          # 全局配置（热键绑定、全局开关等）
├── src/
│   ├── __init__.py
│   ├── ui/                    # UI模块
│   │   ├── __init__.py
│   │   ├── main_window.py     # 主窗口
│   │   ├── script_editor.py   # 脚本编辑窗口
│   │   └── widgets/           # 自定义控件
│   ├── core/                  # 核心逻辑
│   │   ├── __init__.py
│   │   ├── input_simulator.py # 输入模拟（键盘/鼠标）
│   │   ├── script_engine.py   # 脚本执行引擎
│   │   ├── hotkey_manager.py  # 热键管理
│   │   └── sequence_player.py # 序列播放器
│   ├── scripts/               # 用户脚本存放目录
│   │   └── example.py         # 示例脚本
│   └── utils/                 # 工具函数
│       ├── __init__.py
│       └── logger.py          # 日志
└──优秀案例1-quickinput/       # 参考代码
└──优秀案例2-okww/             # 参考代码
模块职责划分（每个模块独立文件，可单独删除/重构）：

模块	职责	依赖
input_simulator.py	封装键盘/鼠标按键模拟，提供press_key(), click_mouse()等接口	无
sequence_player.py	接收一个按键序列列表，按顺序+延迟执行	input_simulator
script_engine.py	加载用户脚本（Python文件），执行其中的run()函数	sequence_player
hotkey_manager.py	注册/注销全局热键，检测鼠标是否在程序窗口内	keyboard/pynput
main_window.py	UI界面，显示脚本列表、绑定热键、设置执行次数	所有core模块

脚本语言”的最终决定
强制建议用Python：你发的Cartethyia代码就是Python，可以直接参考其按键模拟方式用户写脚本就是写Python函数，不需要学习新语言JSON配置作为辅助：对于简单场景（纯按键序列+延迟），提供JSON配置方式，用户不用写代码
两种脚本模式：

模式A：Python脚本（高级）

python
# scripts/my_combo.py
def run(player):
    # player 提供了 press_key(), sleep() 等方法
    player.press_key('1')      # 切1号角色
    player.sleep(0.01)         # 10ms延迟
    player.press_key('e')      # 放E技能
    player.sleep(2.0)          # 等待2秒
    player.press_key('2')      # 切2号角色
    player.sleep(0.01)
    player.press_key('r')      # 放大招
模式B：JSON配置（简单）

json
{
    "name": "三角色连招",
    "keys": ["1", "e", "2", "r", "3", "q"],
    "delays": [10, 2000, 10, 1500, 10, 1000],
    "repeat": 1
}
建议：先实现JSON模式（最简单），再实现Python脚本模式（更灵活）。两者可以共存。

核心功能设计
5.1 热键触发逻辑
你提到的“鼠标不在程序窗口内才能触发”是焦点检测：

python
def is_mouse_over_program_window():
    # 获取当前鼠标位置
    # 获取程序窗口的位置和大小
    # 判断鼠标是否在窗口矩形内
    # 如果在窗口内 → 不触发（避免用户在UI上操作时误触）
    # 如果不在窗口内 → 允许触发
5.2 执行模式（参考优秀案例1）
模式	行为
按下执行	按下热键 → 执行一轮脚本
切换启动/停止	按一次启动（循环执行），再按一次停止
按住执行	按住热键时持续执行，松开停止
松开执行	松开热键时执行一轮

5.3 全局开关
一个“全局禁用”热键，按下后所有脚本的热键绑定失效，再按一次恢复。防止无限循环时失控。

5.4 速度倍率
每个脚本独立配置速度倍率（0.5x、1x、2x等），所有延迟时间乘以该倍率。

六、UI设计原则
参考优秀案例1（Quickinput）的UI风格：简洁、直观、小白友好脚本列表（显示所有已保存脚本）
新建/编辑/删除按钮热键绑定输入框（按下按键自动捕获）执行次数设置（数字输入框 + “无限”复选框）
速度倍率滑块面向小白用户：所有操作通过UI完成，不需要命令行。
关于你发的Cartethyia代码的参考价值
你发的这段Cartethyia代码【用户提供】，核心价值在于：输入模拟方式：self.task.send_key(), self.task.click(), self.task.jump()——这些是你要复用的时间控制：time.time()记录时间点，time.sleep()控制延迟模块化设计：每个角色独立一个类文件，继承BaseChar
但你不需要的：图像识别（cv2、np、find_one、calculate_color_percentage）状态管理（buffs、is_cartethyia、transform）条件分支（if self.liberation_available()等）
你的做法：提取send_key、click等输入模拟方法，抛弃所有图像识别和状态管理代码。
脚本编辑方式
你希望用户如何编写脚本？
A. 内置代码编辑器：在程序内打开一个文本框，用户直接写Python代码，保存后即可使用
B. 外部编辑器：用户在scripts/文件夹里用VSCode/Notepad++编辑.py文件，程序自动加载
C. 两者都要：内置编辑器方便快速修改，外部文件夹方便大规模编辑
我选择C
问题2：JSON配置的复杂程度
对于“纯按键序列”的简单脚本，你希望JSON配置支持到什么程度？
A. 简单：只支持按键列表 + 固定延迟列表
B. 中等：支持按键、延迟、鼠标点击、按住/释放、循环块
C. 复杂：支持条件判断（如果按了X键则跳过Y步骤）
我选择B
问题3：执行中的打断行为
当脚本正在执行（特别是无限循环）时，用户按下停止热键，你希望：
A. 立即停止：中断当前正在执行的步骤，马上停止
B. 执行完当前轮次再停止：当前这一轮完整执行完，然后停止循环
C. 可配置：让用户在设置里自己选
我选择A，如果我绑定的按键设置的是按住就触发，那我分开就让它停止就行。如果确实不可控，那我就按住那个全局停止的按键，这样所有脚本都不会启动
问题4：多脚本同时运行
你是否需要同时运行多个脚本？
A. 不需要：同一时间只能有一个脚本在运行
B. 需要：多个脚本可以同时运行（比如脚本1控制角色1，脚本2控制角色2）
C. 需要，但有优先级：多个脚本同时运行，但可以设置优先级（高优先级脚本先执行）
我选择B，多个脚本可以同时运行的，比如说，我同时按下两个按键，他们就同时执行两个脚本是同步执行的。因为有的时候我要让角色出现在场上，那么第第二个角色会瞬间又出来放进来，那么就实现了两个角色在场上的局面，也有可能是三个角色在场上的局面，这样就比一个角色打出来的伤害多了3倍
问题5：脚本的执行结果反馈
脚本执行时，用户如何知道程序正在运行？
A. 托盘图标变化：系统托盘图标变色/闪烁
B. 状态栏文字：主窗口底部状态栏显示“正在执行...”
C. 日志窗口：显示详细的执行日志（按了哪个键、延迟了多少毫秒）
D. 以上都要
我选择B状态栏文字，它会显示正在执行，我松开按键，它会显示结束执行

我的需求可能没有说明白，你模拟一下你实际做这个项目会遇到什么情况，尽量思考全面，然后你向我提问，最少5个问题，最多无上限。你想的越多越好，这样就能够完善我的需求然后你再问我5个问题吧，帮我完善我的提问。经过一系列的问机器人，我好像把我的需求逐渐的写出来了