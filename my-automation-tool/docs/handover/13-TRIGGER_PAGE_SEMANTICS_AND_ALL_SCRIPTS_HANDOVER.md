# 13 - 给下一位 AI 工作 - 触发页语义与全量脚本交接

> 日期：2026-07-17
>
> Git：本地工作区包含既有 C1/Stage 1 和本轮改动；均未提交、未推送。不得清理、回退或发布。

## 当前唯一任务

让用户按 `../test-plans/STAGE_1_MACRO_OVERVIEW_MANUAL_TEST.md` 进行 Windows/记事本人工验收。通过前只修复该教程可复现的本阶段问题；不得扩展编辑、保存、重载、删除、可配按键或多宏并发。

## 本轮已实现

- 宏页保持 Candy 粉红双栏、脚本选择与无效脚本名红字；不再有启用/停用入口。选中脚本只更新名称只读框，编辑、保存、重新加载、删除全部禁用。
- 触发页完整显示扫描到的每一个 Python 脚本，固定“名称、按键、模式、状态”四列。有效项显示 F9 与切换/按住；无效项名称红字且状态为“不可启用”。
- 只有有效项第 4 列状态可启用/禁用脚本。启用第二项会替换第一项；再次点击当前启用项会停用。它是唯一更新 Python 运行时选中路径的 UI 入口。
- 右侧只读显示 F9、模式、次数、速度和状态。F9/F12、F2 UI 占位、自动检测、活动宏失效停用、OSD 和 fail-closed 未改变。

## 依据与验证

优秀案例 1 源码和根目录第 1、2 张图已确认：宏页管理资源，触发页状态列决定实际可触发集合。本项目只借鉴该语义和布局，未复制其多宏、JSON/QIM、录制、窗口、鼠标或编辑保存功能。

在 `my-automation-tool`：

- `python -m unittest discover -s tests -v`：29/29 通过。
- `python -m compileall -q main.py src macros`：通过。
- `git diff --check`：通过。

已生成离屏触发页截图，确认全部本地脚本按多行显示；离屏环境缺中文字体，且不能代替 Windows 真实输入验收。
