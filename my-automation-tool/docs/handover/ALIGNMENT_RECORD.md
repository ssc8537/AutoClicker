# 源码对齐记录

## 2026-07-18：负责人 /root

- 偏移：首次案例检索错误地报告优秀案例 1 缺少 `trigger.cpp`、`TriggerUi.cpp` 和 `QKeyEdit.h`，未先对真实 `source/` 目录做完整存在性核对。
- 扣分：20 分（流程基准 100，当前 80）。
- 纠正：已确认上述文件均位于 `优秀案例1-Quickinput/Quickinput-main/source/`；后续 Stage 6B 以 `TriggerUi.cpp`、`trigger.cpp`、`thread.cpp`、`QKeyEdit.h` 的真实行为为证据。
- 引以为戒：案例专家已按准确路径完成二次取证；负责人必须先核对文件存在、再作出源码结论。

## 对齐榜样

- 案例专家：在获得准确路径后，以源码行号说明按键命名、实时保存、切换/按下、同键多宏并发和窄右栏布局；未复制案例代码或资源。

## 2026-07-19：负责人 /root

- 偏移：把“状态列直点”按直接调用槽函数的测试结果写成已通过，没有模拟未选中行时的真实鼠标点击；这与优秀案例 1 的 `itemClicked` 行级行为不一致。
- 扣分：10 分（流程基准 100，当前 70）。
- 纠正：案例专家核对 `QMacroTable.h:132-141`、`TriggerUi.cpp:53-65,116-123` 后，补充真实鼠标点击回归；修复详情回填伪保存，以及旧宏首次补写 `ENABLED` 的位置和值。自动验证 71 项通过。
- 引以为戒：涉及 Qt 表格点击时，不能只直接调用槽函数；必须模拟“未选中行 -> 鼠标点击目标单元格 -> 文件实际保存”的完整事件链。
