# Stage 3M Windows 验收归档与 Stage 4T 候选交接

日期：2026-07-18
状态：Stage 3M 已 Windows 验收通过；Stage 4T 仅为下一候选，未 READY

## 已确认事实

用户确认 Stage 3M 验收全部通过：可信 Python 宏的左右键按下/松开、单击、最多 100 次有限重复、最少 10ms 真实间隔，以及 F9/F12/F2 回归均正确。该阶段未验收或实现游戏兼容性、坐标/滚轮/中键/侧键、录制、窗口绑定、图像/游戏判断或多宏。

现有功能分支及草稿 PR #3 的发布记录不变，未合并。用户本地宏 `macros/新宏6.py` 有未提交修改；为确认未覆盖而只读取过 Git 差异，未执行或修改。下一位负责人不得执行、覆盖或未经单独授权提交它。

## 唯一下一候选

Stage 4T：系统托盘。其最小行为、关闭时隐藏还是退出、托盘菜单、恢复方式、F12/活动宏安全关系、启动状态、自动测试和 Windows 验收尚未锁定。下一位负责人必须完成新的对齐与需求门槛、案例/代码取证，获得 READY 后才能实现；独立代码清理和多宏继续排队。

本次只读取证确认：优秀案例参考只允许托盘调用既有 F12 全局状态，不能另建状态机；仓库没有 Stage 4T 规格、实现或测试。当前仅需用户锁定右上角关闭是“安全退出并停止宏”还是“隐藏到托盘并继续运行”；该选择决定后续 F12 和活动宏的安全语义。
