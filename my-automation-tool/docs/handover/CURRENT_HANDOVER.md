# 当前交接：Stage 6B 与 Stage 6D 已 Windows 验收通过；进入版本与反馈阶段

发布快照：用户已授权发布 V022。提交 `5ae0565`（`Release V022: trigger controls and desktop polish`）已推送至 `origin/codex/v021-ui-shell`；草稿 PR #3 仍指向 `master`，未合并。

Stage 6B 按用户目标并参考优秀案例 1 已核实源码实现：触发页可直接录入键盘键或鼠标后退/前进侧键，实时保存热键、模式、次数、速度和每宏启用状态。相同按键的已启用宏允许并发；F12 仍为全局启停。

案例证据：`优秀案例1-Quickinput/Quickinput-main/source/ui/TriggerUi.cpp` 的按键/模式/次数/速度信号均即时 `save()`；`src/trigger.cpp` 遍历全部活动宏，每个宏使用自己的线程。只借鉴行为，未复制源码或资源。

此前状态列“未选中行第一下直点”的回归已修复，用户已确认 Stage 6B 的 1–6 全部通过。根因有二：详情回填热键会伪触发保存并写回旧状态；旧宏缺少 `ENABLED` 时，元数据写入位置和值不正确。现已阻止程序回填触发保存，并以用户点击得到的真实值写入 `ENABLED`。自动验证 73 项通过、6 项旧唯一宏只读断言跳过，编译和差异检查通过；其后 V022 已发布。

最高流程入口：根 `../../AGENTS.md` 的“优秀案例 1 源码先行与零偏移”；本次偏移和纠正见 `ALIGNMENT_RECORD.md`。

本轮 UI 美化与 Per-Monitor V2 DPI 感知已完成 Windows 验收：保留全部页面、控件位置和功能；在 2560×1440、133% 缩放下清晰且底部无裁切。窗口、托盘和 EXE 使用同一粉发头像图标。默认输出目录的 `logs/app.log` 若被外部程序占用，会阻止覆盖构建。

最新事实（2026-07-19）：默认 `dist/MyAutoPlayer/MyAutoPlayer.exe` 的内嵌资源已抽取核验为粉发头像；窗口和托盘也读取同一 `assets/myautoplayer-pink.ico`。若资源管理器仍显示旧图标，这是 Windows 图标缓存，先按 F5 或关闭并重开资源管理器窗口再看。

Stage 6C 的首版 Windows 验收失败：Qt 置前调用不足以保证 Windows 前台，样式箭头也没有实际显示。Stage 6D 改为一次性 Windows 原生前置后立即恢复普通窗口，数字框直接绘制酒红三角；宏库悬停/选中为浅天空蓝且文字保持梅子酒红。案例专家核对优秀案例 1 的音效后，新增默认关闭的“提示音”开关：F12 启用/禁用、宏开始/停止播放两种自制短 WAV，异步播放、资源缺失静默跳过，不复制案例资源或加入外部音频。用户已确认 1–4 全部通过。自动验证 75 项通过、6 项旧唯一宏只读断言跳过，编译和差异检查通过。默认发布目录 `../../dist/MyAutoPlayer/MyAutoPlayer.exe` 已在 2026-07-19 13:12 覆盖为验收通过版。独立验收目录 `../../dist-preview/MyAutoPlayer-Stage6D/` 不影响主程序，但当前环境拒绝递归删除命令；可在不使用时手动删除。当前进入“版本与反馈”阶段。触发边界已锁定：每个宏只对应一个脚本和一个触发键；同一触发键可绑定多个宏并发执行；不做两个键共同触发同一宏。不得从案例扩展 JSON/QIM、录制、坐标、滚轮、识别、窗口绑定、手柄或其他未要求能力。阶段路线见 `../PROJECT_ROADMAP.md`；交给下一位 AI 时直接使用 `NEXT_AI_PROMPT.md`。
