# 当前交接：Stage 7–9 已验收，正在执行 Stage 10–11

用户已确认上一份六步教程 1–6 全部正确：功能页、AI 提示词、复制和单实例均视为 Windows 通过。共享键位的固定中文函数按槽位工作；动作改名后用 `player.按键("改后的名称")`。中文 IME 轻微问题继续作为已接受 bug，不再阻塞。

当前文件清理目标：把全部真实需求合并到根 `PRODUCT_REQUIREMENTS.md`，根 `PROJECT_ROADMAP.md` 保持唯一阶段路线，只保留本交接。旧阶段规格/教程、团队规则、需求演变、重复参考和预览构建将删除；优秀案例源码、源码、测试、用户宏/config 和当前视觉源保留。历史由待发布标签 `VV023-accepted-baseline` 恢复。

Stage 10 当前范围：把两个 AI 提示词从 TXT 改为 UTF-8 Markdown；用漂亮 Markdown 阅读控件渲染；提示词窗口改为可拖到旁边且不阻塞主窗口的非模态窗口；视觉检查用户角色图标透明边距并在不裁切主体的前提下放大；测量启动导入并只优化有证据的同步开销，不能移除 hook ready、安全禁用或清理。

Stage 11 当前范围：将用户提供的 QuickInput JSON 只读转换为一个新的 Python 宏。最终角色名以最后一次明确更正为准：1 秧秧、2 琳奈、3 千咲；旧标记中的卡提、夏空、千笑不使用。必须保留可核对的按键次数和等待，不执行宏，不覆盖现有宏。

默认不提交/推送新 Stage 10–11 改动；先发布用户已验收的 VV023 基线。工作区包含用户 `macros/`、`config/`，不得执行、覆盖或清理。
